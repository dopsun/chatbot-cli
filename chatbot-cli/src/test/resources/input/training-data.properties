# get_contact
get_contact = get contact ${contact-hint}
get_contact = contact ${contact-hint}

# schedule_meeting
schedule_meeting = meet ${contact-hint} at ${time-hint}

# get_order_for_client
get_order_for_client = get order for client ${client-hint}
